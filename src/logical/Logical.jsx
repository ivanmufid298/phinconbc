import React from 'react'

const Logical = () => {
    const a = 3
    const b = -2

    console.log(a > 0 || b > 0)
  return (
    <div>{console.log(a > 0 || b > 0)}</div>
  )
}

export default Logical